import type { Testimonial } from '../types'

// A Codify ainda não tem cases publicados com depoimento de cliente.
// Assim que o primeiro projeto for concluído, adicione o objeto aqui —
// o layout em SocialProof.tsx já está pronto para renderizar a lista.
export const testimonials: Testimonial[] = []
